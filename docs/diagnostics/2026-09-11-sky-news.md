# Sky News: existing-log assessment — 11 September 2026

**Follow-up:** [Direct inspection of the Sky News export](2026-09-11-sky-news-export.md) subsequently confirmed zero audio samples in three complete fragments served by the iPhone. The assessment below predates that capture.

Read-only collection at 14:05 BST verified the active Pi release as `20260910T213612844711Z-52c2997e3800-dirty`, receiver PID 1046, zero automatic restarts. No receiver configuration or playback was changed.

## Latest attempt

The latest request began at 14:02:10 BST: current boot `f830a6e19219451eb7bd756c5adf40d9`, session 7, child PID 1157. The user identifies the last channel as Sky News; channel names are deliberately absent from the structured report. Its route was direct HTTP. The final logged startup snapshot was at 14:02:12.

Every snapshot has `packet_capture_enabled=0`. Therefore its zero audio and video packet counters are unmeasured, not evidence of absent media. Periodic snapshots and packet tracing are enabled only by the receiver's debug screen mode; ordinary mode logs state changes. The early snapshot also lacks initialized codec/audio metadata, as do startup snapshots of successful preceding attempts. It cannot establish the later state of Sky News.

Preceding direct HTTP sessions 5 and 6 did initialize AAC, 48 kHz stereo, ALSA and advancing audio positions. This supports functioning audio output on the same current receiver, but does not establish Sky News packet delivery.

## Relevant earlier evidence

The retained 10 September HEVC report independently verifies a completed 30.068-second capture with 1,250 selected video packets (9,238,633 bytes) and zero selected audio packets. AAC track 1 was selected but never initialized. Packet capture errors, rejected packet grammar and log/event overflows were zero. These are selected demux queue counts, not raw incoming audio/video byte inspection. That older item has not been identified as Sky News.

The separate earlier GStreamer investigation recorded explicit sender audio teardown before video, complete fragments needing missing-audio repair, and no compressed/decoded/sink audio, followed by audible YouTube. This supports a recurring missing-audio problem before the audio decoder, but does not conclusively assign responsibility to UHF rather than its input or receiver demux/track selection.

## Decision

UHF's video export is a credible suspect; the existing logs do not prove a UHF bug for the latest Sky News attempt. Pause speculative decoder/output changes. A decisive focused comparison would inspect actual audio samples in UHF's exported media for the same channel, alongside confirmation of sound in UHF locally. A repeat with packet tracing would establish whether Sky News has the earlier selected-queue symptom, but cannot alone distinguish missing source/export audio from demux extraction failure.

## Evidence

- Current sanitized report (local `.pi-dev/sky-news-playback-report-20260911.json`)
- Earlier complete packet report (local `.pi-dev/playback-report-hevc-packet-trial-followup.json`)
- [Earlier GStreamer audio investigation](2026-09-10-uhf-audio.md#current-candidate-activated-silent-uhf-reproduced-with-complete-audio-trace)

The current export retained 542 records without input/export truncation or record-limit loss. Two rejected records were playback-session records at 14:01:17–18, before Sky News; neither was an mpv playback/packet snapshot. Historical retained records belong to the previous boot and must not be assigned the current runtime identity.
